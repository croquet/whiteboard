{
  "name": "croquet-whiteboard",
  "author": "Croquet Corporation",
  "license": "Apache 2.0",
  "scripts": {
      "setup": "curl -L -o croquet/croquet.min.js https://unpkg.com/@croquet/croquet; curl -L -o croquet/croquet-virtual-dom.js https://unpkg.com/@croquet/virtual-dom"
  }
}
